spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: springuser
    password: 19961111..
    url: jdbc:mysql://localhost:3306/mall?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  redis:
    host: 127.0.0.1
    port: 6379
  rabbitmq:
    addresses: 192.168.198.128
    port: 5672
    username: guest
    password: guest

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    #控制台日志配置
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mappers/*.xml

logging:
  pattern:
    console: "[%thread] %-5level %logger{36} - %msg%n"


server:
  servlet:
    session:
      #2min  120s
      timeout: 120


